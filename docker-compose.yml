services:
  ai-bridge:
    build: .
    ports:
      - "4567:4567"
    environment:
      PORT: 4567
      AIBRIDGE_DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
